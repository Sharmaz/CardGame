{
	"scripts": {
		"jade": "jade --watch --pretty lib/*.jade --out .",
		"stylus": "stylus --use nib --watch lib/app.styl --out public/css",
		"server": "http-server .",
		"babel": "babel --watch lib --out-dir public",
		"build": "browserify -t babelify lib/app.js > public/app.js"
	}
}